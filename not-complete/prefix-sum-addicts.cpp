#include<bits/stdc++.h>

using namespace std;

int main() {
    int n; cin >> n;

    while(n--) {
        int length, k; cin >> length >> k;
        vector<int> nums(k);

        for (int i=0; i<k; i++) cin >> nums[i];
    }


    return 0;
}