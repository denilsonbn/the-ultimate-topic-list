#include <bits/stdc++.h>

using namespace std;

void path(int t, int on) {
    vector<int> no_paths(on);

    

}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(0);

    int t, on;
    cin >> t >> on;

    cout << t-1 << "\n";
    path(t, on);

    return 0;
}